services:
  vaultwarden:
    image: vaultwarden/server:1.35.4
    container_name: vaultwarden
    restart: unless-stopped
    env_file:
      - /home/ubuntu/services/vaultwarden/.env
    ports:
      - "127.0.0.1:8082:80"
    volumes:
      - /home/ubuntu/services/vaultwarden/data:/data
